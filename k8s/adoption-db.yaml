apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adoption-db-pvc
  namespace: adoption-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adoption-db
  namespace: adoption-system
spec:
  replicas: 1

  selector:
    matchLabels:
      app: adoption-db

  template:
    metadata:
      labels:
        app: adoption-db

    spec:
      containers:
        - name: postgres
          image: postgres:17

          ports:
            - containerPort: 5432

          env:
            - name: POSTGRES_DB
              value: adoption_db

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: database-secret
                  key: username

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database-secret
                  key: password

          volumeMounts:
            - name: adoption-data
              mountPath: /var/lib/postgresql/data

      volumes:
        - name: adoption-data
          persistentVolumeClaim:
            claimName: adoption-db-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: adoption-db
  namespace: adoption-system
spec:
  selector:
    app: adoption-db

  ports:
    - port: 5432
      targetPort: 5432